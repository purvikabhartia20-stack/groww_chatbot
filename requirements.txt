# ============================================================
# Mutual Fund FAQ Assistant — Python Dependencies
# ============================================================
# Pin exact versions for reproducibility and supply-chain safety.
# Run: pip install -r requirements.txt
# Then: playwright install chromium

# ---------------------------------------------------------------------------
# Phase 1 — Data Collection
# ---------------------------------------------------------------------------
playwright==1.44.0              # headless browser for JS-rendered Groww pages

# ---------------------------------------------------------------------------
# Phase 2 — Document Processing & Chunking
# ---------------------------------------------------------------------------
beautifulsoup4==4.12.3          # HTML parsing
lxml==5.2.2                     # fast HTML/XML parser backend for BS4
pdfplumber==0.11.0              # PDF text + table extraction
PyMuPDF==1.24.3                 # PDF extraction fallback (fitz)
pytesseract==0.3.10             # OCR fallback for image-based PDFs
Pillow==10.3.0                  # image handling for pytesseract
nltk==3.8.1                     # sentence tokenization for safe chunking
spacy==3.7.4                    # NLP: sentence segmentation, NER
tiktoken==0.7.0                 # token counting (OpenAI-compatible)
langdetect==1.0.9               # language detection (EC-2.8, EC-4.9)

# ---------------------------------------------------------------------------
# Phase 3 — Embedding & Vector Store
# ---------------------------------------------------------------------------
# NOTE: Groq does not provide an embeddings API.
# Embeddings are generated locally using sentence-transformers (no API key needed).
sentence-transformers==3.0.1    # local embeddings: BAAI/bge-small-en-v1.5 (384-dim)
chromadb>=1.5.0                 # local vector store — 1.x ships pre-built wheels on Windows (no MSVC needed)
torch==2.11.0                   # required by sentence-transformers

# ---------------------------------------------------------------------------
# Phase 4 — RAG Pipeline (Groq LLM)
# ---------------------------------------------------------------------------
groq>=1.2.0                     # Groq API client — 1.x required for httpx 0.28 compatibility

# ---------------------------------------------------------------------------
# Phase 5 — Frontend & API
# ---------------------------------------------------------------------------
fastapi==0.111.0                # REST API backend
uvicorn==0.30.1                 # ASGI server for FastAPI
streamlit==1.35.0               # chat UI frontend
httpx==0.27.0                   # async HTTP client (used in FastAPI)
pydantic==2.7.1                 # request/response validation

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
python-dotenv==1.0.1            # load .env for API keys
tenacity==8.3.0                 # retry logic with exponential backoff
